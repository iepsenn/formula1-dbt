
version: 2

models:
  - name: drivers
    description: "Drivers Table"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
